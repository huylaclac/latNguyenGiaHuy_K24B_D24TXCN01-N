#include<stdio.h>
    int main() {
       int sogoc=50, answer;
       do{
       printf("nhap vap dap an mot so nguyen: ");
       scanf("%d", &answer);
       } while(answer != sogoc);
       printf("chuan r ");
       
    return 0;
}
